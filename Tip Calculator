print("Welcome to the Tip Calculator!")
bill = input("What was the total bill? ")

total_bill = float(bill)

tip = input("How many % tip would you like to give? 10, 12, or 15? ")

total_tip = int(tip)/100

people = input("How many people to split the bill? ")
total_people = int(people)

final_tip = round(total_bill + total_tip/total_people,2)
print(f"Each person should pay ${final_tip}")
