# Banker's Roulette

names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")

import random

length = len(names)

element = random.randint(0,length-1)

print(f"{names[element]} is going to buy the meal today!")
